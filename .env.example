# Taonix 環境變數設定範例
# 複製此檔為 .env 並填入你的設定值

# 資料存放根目錄（預設為專案目錄）
# TAONIX_DATA_DIR=/path/to/custom/data/dir

# AI Provider 設定
# 預設使用 opencode（需先安裝：npm i -g opencode）
# 如需使用 Ollama，在程式碼中設定 provider: "ollama"

# opencode 使用的 model 由 opencode 自身設定管理
# 參見 opencode 文件：opencode run --help

# Web 控制台連接埠（預設 3000）
# TAONIX_WEB_PORT=3000
